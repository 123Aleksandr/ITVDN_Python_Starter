#Напишите программу, которая решает квадратное уравнение
#ax**2 + bx + c = 0
#по формулам X1,2 = (-b±(корень из b**2 − 4ac))/2a. Значения a, b и c
#вводятся с клавиатуры. Для извлечения корня используйте оператор
#возведения в степень, а не функцию math.sqrt, чтобы получить комплексные
#числа в случае, если подкоренное выражение отрицательно.
#(корень из b**2 − 4ac) = (b**2 - 4ac)**1/2


a = float(input('Введите \'a\' и нажмите Enter:'))
b = float(input('Введите \'b\' и нажмите Enter:'))
c = float(input('Введите \'c\' и нажмите Enter:'))

d = ((b**2) - 4 * a * c)
D = (d**(1/2))

x1 = (- b + D)/(2 * a)
x2 = (- b - D)/(2 * a)

if d == 0:
    print('Так как D = 0 уравнение имеет один корень:', x1)
elif d > 0:
    print(x1, x2, sep = ', ')
else:
    d < 0
    print('''D < 0, корней нет, т.к. квадрат не может быть
равен отрицательному числу.''')
